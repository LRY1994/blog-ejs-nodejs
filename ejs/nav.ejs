<div class="nav-img">
    <img src="../static/img/avatar.png"/>
</div>


<ul>
    <% navList.forEach(function(item){%>
        <% if (categories==item.name){ %>
            <li class="li-active"> <a href="<%= item.path %>"><%=item.name %></a></li>
        <%} else{%>
            <li > <a href="<%= item.path %>"><%=item.name %></a></li>
        
    <% }}) %>
</ul>